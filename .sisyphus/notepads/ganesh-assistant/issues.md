## Environment Issues
- cargo and rustc binaries are missing from the environment, preventing verification of Tauri build/check steps.

## Wave 2: Tauri Shell (Task 9)
- `cargo`/`rustc` were missing from PATH but ARE installed at `~/.cargo/bin/`. Add to PATH before running cargo commands.
- Real Node.js required for vitest (bun's node shim breaks worker pools). Downloaded node v20.11.1 to /tmp/node-v20.11.1-linux-x64/bin. Not persistent across sessions — re-download or install system node.
- `tauri-build = "2.11.5"` (copied from tauri version) does NOT exist on crates.io. Fixed to `2.6.3`. Spec said "pinned to 2.11.5" for tauri (correct) but didn't specify tauri-build version.
- Placeholder sidecar stub at `backend/dist/ganesh-backend-x86_64-unknown-linux-gnu` is a shell script, NOT a real binary. Must be replaced by `pyinstaller` build before bundling. main.rs falls back to `python3 backend/main.py` in dev when the bundled binary isn't next to the exe.
- Placeholder icons in `src-tauri/icons/` are solid-color PNGs generated by a python script. Replace with real Ganesh branding before release.
- vitest `act()` warning in `test_use_sidecar > stays not-ready when port is null` — harmless (background retry timer fires after assertion). Could be silenced by advancing timers / unmounting, but test passes.
## Git Master Issues
- Encountered issues with `.gitignore` preventing `git add` of the plan file. Fixed by correcting the ignore patterns.
- Found untracked evidence files in `frontend/.sisyphus/`. Fixed by updating `.gitignore` to ignore `.sisyphus` directories globally except for plans and notepads.

## Task 41 Resume (2026-07-07)
- Notepad files restored from eba7925 and current runbook info re-appended.
- Uncommitted changes from aborted Task 41 attempt need cleanup and commit.

## Task 41 Resume (2026-07-07)
- Notepad files restored from eba7925 and current runbook info re-appended.
- Uncommitted changes from aborted Task 41 attempt need cleanup and commit.
